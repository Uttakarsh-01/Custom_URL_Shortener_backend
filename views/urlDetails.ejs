<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Details</title>
</head>
<body>
    <h1>URL Details</h1>
    <p><strong>Short ID:</strong> <%= url.shortId %></p>
    <p><strong>Original URL:</strong> <a href="<%= url.redirectURL %>"><%= url.redirectURL %></a></p>
    <p><strong>Visits:</strong> <%= url.visitHistory.length %></p>
    <h3>Visit History</h3>
    <ul>
        <% url.visitHistory.forEach((visit) => { %>
            <li><%= new Date(visit.timestamp).toLocaleString() %></li>
        <% }) %>
    </ul>
</body>
</html>
